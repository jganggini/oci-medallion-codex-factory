# Quality Contracts - trafico-datos

Artefactos para QA funcional y tecnico del proyecto medallion.

- `contracts/`: contratos JSON por dataset y capa
- `sql/`: queries de validacion para Autonomous Database
- `samples/`: archivos pequenos para demos locales y smoke tests

Los contratos se ejecutan con `oci-data-quality-mcp`.
