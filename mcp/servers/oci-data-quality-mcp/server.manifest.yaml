name: oci-data-quality-mcp
runtime: python
service: quality
mirror_output: workspace/oci-mirror/<env>/<compartment>/quality/
expected_tools:
  - profile_bucket_data
  - run_quality_contract
  - gate_migration
